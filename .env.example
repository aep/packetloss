# Copy to .env (repo root). The builder loads it via dotenv.
# Create a key at https://atlas.ripe.net/keys/ with the
# "Create a new user defined measurement" permission (+ account credits).
RIPE_ATLAS_API_KEY=
